<%= form_for @contrato, :html => { :class => 'form-horizontal' } do |f| %>

  <div class="control-group">
    <%= f.label :clave, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :clave, :class => 'text_field' %>
    </div>
  </div>
  <div class="control-group">
    <%= f.label :contenido, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_area :contenido, :class => 'text_field' %>
    </div>
  </div>
  <div class="control-group">
    <%= f.label :cliente, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :cliente, :class => 'text_field' %>
    </div>
  </div>

  </div>
  <div class="control-group">
    <%= f.label :audio, :class => 'control-label' %>
    <div class="controls">
      <%= f.file_field :audio, :class => 'file_field' %>
    </div>
  </div>
  <div class="form-actions">
    <%= f.submit nil, :class => 'btn btn-primary' %>
    <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                contratos_path, :class => 'btn' %>
  </div>
<% end %>
